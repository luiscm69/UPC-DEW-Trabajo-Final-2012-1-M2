<%= form_for(@owner_patient) do |f| %>
  <% if @owner_patient.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@owner_patient.errors.count, "error") %> prohibited this owner_patient from being saved:</h2>

      <ul>
      <% @owner_patient.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :owner_id %><br />
    <%= f.number_field :owner_id %>
  </div>
  <div class="field">
    <%= f.label :patient_id %><br />
    <%= f.number_field :patient_id %>
  </div>
  <div class="field">
    <%= f.label :note %><br />
    <%= f.text_area :note %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
