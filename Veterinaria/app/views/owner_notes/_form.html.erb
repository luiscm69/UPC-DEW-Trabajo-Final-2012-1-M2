<%= form_for(@owner_note) do |f| %>
  <% if @owner_note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@owner_note.errors.count, "error") %> prohibited this owner_note from being saved:</h2>

      <ul>
      <% @owner_note.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :owner_id %><br />
    <%= f.number_field :owner_id %>
  </div>
  <div class="field">
    <%= f.label :note_id %><br />
    <%= f.number_field :note_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
